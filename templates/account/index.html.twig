{% extends 'base.html.twig' %}

{% block title %}The French Shop!{% endblock %}

{% block content %}
<div class="container">
     {% if is_granted('ROLE_ADMIN') %}
          <small><a href="{{ path('admin') }}">Vous êtes administrateur, accèder au backoffice</a></small><br/>
     {% endif %}
     <h1>Mon compte</h1>
     Bienvenue {{ app.user.firstname }} dans votre compte.<br> C'est dans cet espace que vous aller pouvoir gérer toutes vos informations personnelles.
     <hr>
     <div class="row">
          <div class="col-md-4">
               <div class="account-item text-center">
                    <a href="{{ path('account_password') }}">Modifier mon mot de passe</a><br/>
               </div>
          </div>
          <div class="col-md-4">
               <div class="account-item text-center">
                    <a href="{{ path('account_address') }}">Gérer mes adresses</a><br/>
               </div>
          </div>
          <div class="col-md-4">
               <div class="account-item text-center">
                    <a href="{{ path('account_order') }}">Mes commandes</a><br/>
               </div>
          </div>
     </div>
</div>
{% endblock %}
